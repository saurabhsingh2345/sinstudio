# media (projects, assets, renders) — user data, not source.
# Anchored to the repo root so it does NOT match the Go package
# backend/internal/media/.
/media/

# frontend
frontend/node_modules/
frontend/dist/

# go
backend/studio
backend/*.out
go.work.sum

# whisper models (auto-discovered by the backend; download separately)
/models/

# misc
.DS_Store
*.log

# Lovable design reference (separate git repo — reference only, do not commit)
studio-frontend/
# stray root lockfile
/package-lock.json

# Built helper binary — tools/cursord is source; build it locally.
/tools/cursord/cursord
